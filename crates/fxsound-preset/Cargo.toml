[package]
name = "fxsound-preset"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Reader, writer and store for FxSound .fac preset files"

[dependencies]
fxsound-core = { workspace = true }
thiserror = { workspace = true }
log = { workspace = true }

[dev-dependencies]
# Only the `write_preset` example needs these. The library itself stays free of serde: a crate
# whose job is one hand-written text format has no business pulling in a derive macro.
serde = { workspace = true }
serde_json = { workspace = true }
