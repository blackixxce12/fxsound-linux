# Cargo build output
/target/
# makepkg working directories and the built package
/packaging/pkg/
/packaging/src/
/packaging/*.pkg.tar.zst
/packaging/*.log
# editor / OS noise
*.swp
.DS_Store
/dist/
# Claude Code session state, never part of the source tree
/.claude/
